local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "typescript", "javascript" , "gopls"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
